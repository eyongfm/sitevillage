{% extends 'base.html.twig' %}   {# inherites from the base.html.twig file - inherits the nav bar and all the css contained in this file #}

{% block body %}
    {# <article>
            <h2>Bienvenue dans page Evenements</h2>
            <div class="metadata">Ecrit le 19/04/2021 à 07:00 dans la catégorie Politique</div>
            <div class="content">
                <img src="{{ event.image }}" alt="">
                <br />
                {# replace the two paragraphs here with interpollation ... #}
                {#{ event.content | raw }} {# filter the content with a twig filter called: raw so that it will not print out the html <p> tags
            </div>
        </article> #}

   

    <h2>____________ La suite de votre l'actualité _________________</h2>

  

    <div class="row p-5">
        <div class="col-sm-5 m-1">
         <div class="card col-m-3">
         <h4 class="card-header text-white bg-info">{{ event.title | raw }}</h4>
            <div class="metadata text-center"> Ecrit le {{ event.createdAt | date('d/m/Y') }} à {{ event.createdAt | date('H:i') }} dans la catégorie evenement </div> 
            {# <div class="metadata">Ecrit le 19/04/2021 à 07:00 dans la catégorie Politique</div> #}
             <div class="card-body">
             <img src="{{ event.image }}" style="width: 100%; height:100%;" alt="">
             </div>
          </div>     
        </div>
        <div class="col-sm-6 border border-success m-1">
            <br /> <br /> 

            {{ event.content | raw }}
            
            <br /> <br />

             <a href="{{ path('evenement') }}" class="btn btn-primary">Retour sur la page d'événements</a>
        </div>
    </div>  


{% endblock %}

